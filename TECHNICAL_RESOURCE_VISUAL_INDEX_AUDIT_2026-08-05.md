# 技術資源與視覺索引稽核｜2026-08-05

## 本輪完成

- 三語 `technical-resources` 頁新增視覺資源索引。
- 每個語言頁有 6 張目前網站已存在的產品視覺參考，以及 6 個對應產品頁入口。
- 視覺卡片明確標示：圖片不是正式 SKU 或完整規格，正式資料仍需依產品、圖面與版本確認。
- 本地檢查：三語頁各 `marker=1`、`cards=6`、產品入口 `6`；產品頁品質與案例頁合約檢查仍為 `48/48`、`72/72`。

## 資料可用性判定

本次在網站專案與指定營運資料子目錄進行有界檔案盤點。找到可直接公開的產品圖片與代表性尺寸圖，但沒有找到可直接放入公開下載庫且已確認版本／授權的 CAD、DWG、DXF 或 STEP 原始檔。網站因此保留以下分流：

1. 代表性尺寸圖：可直接查看。
2. PDF／CAD／DWG／DXF／STEP：透過詢價表單依 SKU、版本與專案條件索取。
3. 材質／表面處理：依正式圖面、報價與樣品確認。
4. 打樣可行性：提供尺寸、數量、時程、交貨地與照片／圖面後逐案確認。

## 公開狀態

- Git `main` 已提交 `918eedf`，並同步 `draft`、`release/draft`；網站內容變更提交為 `318c6b3`。
- 公開三語技術資源頁以 cache-buster 讀回均為 HTTP 200，且各自讀到 1 個 `data-bf-resource-visual-index="1"`、6 張視覺卡片、6 個對應產品入口與正確語言 canonical。
- 六個公開圖片資源均回應 HTTP 200；因此本輪視覺索引已完成公開驗證。
- 手機專用 viewport 本次瀏覽連線未成功套用；CSS 的 `max-width:768px` 規則已確認將 `grid-3` 改為單欄，手機獨立 runtime 仍沿用既有手機驗收紀錄，不以本次桌機讀回替代。
